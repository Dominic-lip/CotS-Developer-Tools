# Foundation Completion Ledger

Reconciled: 2026-09-02. This is the authority for foundation tasks and the
production roadmap's evidence-backed task state; the machine-readable scheduler
input is `Docs/FOUNDATION_COMPLETION_STATE.json`.
`COMPLETE_VERIFIED` means the stated acceptance evidence is committed and
inspectable. `COMPLETE_BUT_EVIDENCE_MISSING` preserves the implementation but
blocks advancement until the missing acceptance evidence is recorded. `PARTIAL`
and `NOT_STARTED` also block advancement. No status is inferred from a task
file, an agent's outcome marker, or a later task number.

| Task | Status | Acceptance criteria | Implementation / commit evidence | Test / live Unreal evidence | Missing work and further execution |
| --- | --- | --- | --- | --- | --- |
| TASK-000 | COMPLETE_VERIFIED | Explicit, non-conflicting boundaries and agent-neutral/no-destructive rules. | `e9f3a61`; `AGENTS.md`, `Docs/ARCHITECTURE.md`, task/spec templates. | Repository policies are the durable deliverable; no runtime proof applies. | None. |
| TASK-001 | COMPLETE_VERIFIED | Both CLIs consume one task from intended working directory. | `e9f3a61`, `396a5cc`, `e4ed798`, `73ebf49`; `Scripts/Run-CotSTask.ps1`. | `Docs/Validation/TASK-001_RUNNER_PROOF.md` records the Claude runner `-DryRun` and harmless-read transcript plus the 2026-08-30 direct active-Codex-supervisor-adapter harmless-read proof; both report `# CotS Developer Tools` with no task-proof mutations. | None. The nested Codex CLI socket limitation is an unsupported recursive validation topology under `Docs/AUTONOMOUS_DEVELOPMENT.md`, not a remaining acceptance failure. |
| TASK-002 | COMPLETE_VERIFIED | Clean Tool Lab editor build/startup and initial console reads. | `e9f3a61`, `4a7fe48`, `9dcd722`; ToolLab project/plugin/baseline scripts. | `Docs/Validation/TASK-002_TOOLLAB_PROOF.md` records the fixed Host canonical build (UBT `Result: Succeeded`), native editor readiness, loaded plugin/module, `/Game` listing, valid/invalid exact asset reads, and graceful close. | None. |
| TASK-003 | COMPLETE_VERIFIED | Codex and Claude independently inspect the same native Unreal MCP editor. | `e9f3a61`; current root and ToolLab MCP endpoint configuration (`.mcp.json`, `ToolLab/.mcp.json`), now checked-in and persistent rather than a local `claude mcp add`. | `Docs/MCP_CAPABILITY_MATRIX.md` records Codex's proof; `Docs/Validation/TASK-003_MCP_CONNECTIVITY.md` records the prior turn's session-start ordering finding (ToolLab must already be running for Claude's MCP client to connect) and this turn's completed Claude proof: `list_toolsets`/`describe_toolset` enumeration, engine version `5.8.1-56057345+++UE5+Release-5.8`, project `CotSToolLab`, current level `/Temp/Untitled_1` with no actor selected — matching Codex's values, confirming both clients inspected the same running editor. | None. |
| TASK-004 | COMPLETE_VERIFIED | Matrix grounded in real calls, including standby critical-read compatibility sample. | `9305a79`, `e4763c2`; `Docs/MCP_CAPABILITY_MATRIX.md`. | Codex's full mutation/readback/cleanup audit is recorded, plus a 2026-08-30 Claude standby critical-read sample (`Docs/MCP_CAPABILITY_MATRIX.md` update note): viewport camera transform, Automation discovery, `find_assets`, and log retrieval, all read-only and all matching Codex's classifications against the same running editor. `Docs/Validation/TASK-003_MCP_CONNECTIVITY.md` covers the project/level/toolset-enumeration portion. | None. |
| TASK-005 | COMPLETE_VERIFIED | Stable editor-only plugin base, clean build/startup and plugin automation test. | `d80f82f`; result core, domain interfaces, foundation test. | `Docs/Validation/TASK-005_PLUGIN_FOUNDATION.md` records a clean canonical `Build-ToolLab.cmd` (`Result: Succeeded`) and a full `RunCotSAutomation` pass: 12/12 `CotS.*` tests succeeded (`TEST COMPLETE. EXIT CODE: 0`), including per-domain `ToolRegistration` tests that directly prove editor startup and module/toolset load for Foundation, Inspection, Execution, Mutation, Validation, and Lifecycle. | None. |
| TASK-006 | COMPLETE_VERIFIED | Exact asset/dependency/animation/duplicate-name inspection without clicking. | `2ebfab0`; `CotSInspectionToolset`. | `Docs/Validation/TASK-006_INSPECTION_FOUNDATION.md` cites the `CotS.Inspection.ExactPathsAndEmptyReferences` automation pass (reused from the same TASK-005 run, unchanged source): two identically-named disposable assets in different folders are searched, flagged as duplicates, and dependency/referencer-queried by exact path without confusion. | None. |
| TASK-007 | COMPLETE_VERIFIED | One harmless novel inspection through an auditable development-only bridge. | `a9d1d94`; `CotSExecutionToolset`, `Docs/EXECUTION_BRIDGE.md`. | The document records the source audit and constrained live-proof behaviour. | None. |
| TASK-008 | COMPLETE_VERIFIED | Guarded mutation primitives plus independent reinspection in a disposable namespace. | `b44a203`; `CotSMutationToolset`, plugin tests. | `Docs/Validation/TASK-008_SAFE_MUTATION_PRIMITIVES.md` cites the `CotS.Mutation.AssetWorkflowAndGuardrails` and `CotS.Mutation.ActorWorkflow` automation passes (reused from the same TASK-005 run, unchanged source): create/move/duplicate/property-mutate/save for assets and create/transform/component-add-remove/delete for actors, each independently re-inspected in `/Game/CotSMutationLive`. | None. |
| TASK-008A | COMPLETE_VERIFIED | Lock/open/close/build/test/reopen/read lifecycle proof. | `4127983`; Host MCP and lifecycle toolset. | `Docs/Validation/TASK-008A_LIFECYCLE_PROOF.md` records the full acquire-lock/open/close/build/test/reopen/wait/harmless-inspect/close/reopen/verify/release sequence with exact PIDs, operation results, and a 12/12 automation pass, all against the live Host MCP and native Unreal MCP. | None. |
| TASK-008B | COMPLETE_VERIFIED | Durable, leased Codex continuation supervisor. | `e9b3732`; supervisor, launcher and checkpoint/lease implementation. | `Docs/Validation/TASK-008B_PERSISTENT_SUPERVISOR_PROOF.md` records live evidence from `.cots/supervisor-events.log` and `.cots/factory-controller.local.json`: 8 real process restarts today with checkpoint-driven task continuity strictly advancing (TASK-004→005→006→008A), two factory-controller-detected crash recoveries (`RECOVERABLE_STALE_STATE`, `RECOVERABLE_SUPERVISOR`) that resumed cleanly, and the spec-required failed-resume-starts-fresh-session fallback observed live. | None. |
| TASK-008C | COMPLETE_VERIFIED | Dashboard, provider rotation and single-mutator boundary. | `ba71b42`, `02590ab`, `8ecf9f5`, `85187f0`, `700d648`, `aca1869`. | Deterministic tests and captured Claude/Codex protocol fixtures document the acceptance behaviour. | None. |
| TASK-009 | COMPLETE_VERIFIED | Detect a disposable validation failure, repair it and prove clean result. | `51fdfef`; `CotSValidationToolset`. | `Docs/VALIDATION_DIAGNOSTICS.md` records missing CurveFloat -> create -> validate -> delete evidence. | None. |
| TASK-010 | COMPLETE_VERIFIED | Deterministic Tool Lab test launches, observes runtime state, stops, returns pass/fail. | Native PIE/Automation coverage recorded in `e4763c2`; runtime readers added in `5bb14ed`, `028b81d`. | `Docs/Validation/TASK-010_AUTOMATED_TESTING.md` cites the TASK-008A `RunCotSAutomation` pass (reused, unchanged source): unattended launch, live-state Automation Controller execution, deterministic `-TestExit`-driven clean termination, and `TEST COMPLETE. EXIT CODE: 0` with 12/12 `Result={Success}`. | None. |
| TASK-011 | COMPLETE_VERIFIED | A Tool Lab mutation produces a reviewable before/after report. | `e9b3732`; `Scripts/CotS-GitCompletion.py` and supervisor restrictions. | `Docs/Validation/TASK-011_CHANGE_GUARDRAILS.md` records source preflight/after status, a guarded CurveFloat preview/create/mutate/save/inspect sequence, an intentional out-of-scope delete refusal, approved-scope cleanup, and graceful lifecycle release. | None. |
| TASK-012 | COMPLETE_VERIFIED | Identical autonomous Blueprint/map/PIE proof once each by Codex and Claude. | `5bb14ed`, `028b81d`; Codex proof assets `/Game/CotSAutonomousProof/BP_CodexProofActor` and `Maps/M_CodexProof`. | `Docs/Validation/TASK-012_CLAUDE_AUTONOMOUS_PROOF.md` records the independent Claude proof: `BP_ClaudeProofActor` (visible `ProofSphere` component, instance-editable `RuntimeValue` float) placed in disposable `Maps/M_ClaudeProof`, compiled/saved, PIE-verified live runtime value `777.5` matching the configured instance override, clean PIE stop, and full log/asset report — all via native Unreal MCP with no manual construction. | None. |
| TASK-013 | COMPLETE_VERIFIED | Full MetaHuman-compatible locomotion authoring and test (retarget, Blend Space, AnimBP/state machine, policy, validation). | Adds guarded `CreateDisposableIKRig` and `CreateDisposableIKRetargeter` operations over UE 5.8's public controller/factory APIs, alongside the existing skeleton, animation, retarget, Blend Space, AnimBP/state-machine, policy, and runtime-inspection toolset. All creation remains restricted to `/Game/CotSMutationLive/`. | `Docs/Validation/TASK-013_LOCOMOTION_CONTENT_PREREQUISITE.md` records the complete proof: canonical build `Result: Succeeded`; native-MCP creation of a 29-chain Mannequin IK Rig; a Retargeter joining it to the distinct `/MetaHumanCharacter/.../SKM_Body`; native batch retarget of `MM_Idle`; independent inspection confirming the output uses `metahuman_base_skel`; graceful close; and 13/13 `CotS` automation successes (`TEST COMPLETE. EXIT CODE: 0`). | None. |
| TASK-014 | COMPLETE_VERIFIED | Classified Shardlands inventory with dependencies and confidence. | Read-only UE 5.8 donor inventory of modules, plugins, config, C++, content/map roots and canonical data pipeline. | `Docs/Validation/TASK-014_SHARDLANDS_DONOR_AUDIT.md` classifies character/MetaHuman, animation, inventory, interaction, combat, world, UI, maps/experiments, development tools and canonical data as direct migration, cleanup, rebuild or retain-in-donor; it records dependencies and confidence. | No donor or CotS mutation performed. |
| TASK-015 | COMPLETE_VERIFIED | Clean UE 5.8 production project/baseline with client/editor build and bootstrap architecture. | `fb6c12c`, `821bfe6`, `f90da9c`, and `d6557e6` in `C:\Dev\CotS`: fixed bootstrap, native-MCP configuration, a GameFeatureData rule, and saved `/Game/Maps/CotS_Entry`. The bridge has bounded map-creation, map-inspection, and smoke commandlet operations. | `Docs/Validation/TASK-015_PRODUCTION_BOOTSTRAP.md` records editor UBT success, game build success, server-target refusal, native MCP readiness, direct native map inspection/load, map creation commandlet exit 0, graceful close, and the fixed smoke commandlet's exit-0 map-load proof. | None. The smoke applies a per-commandlet MCP auto-start override so its transient editor never collides with the normal editor's port-8000 listener. |
| TASK-016 | COMPLETE_VERIFIED | Both adapters, in one session, use Host + native Unreal MCP and prove lock contention/read-only connectivity. | `4748427`, `585dfee`, `04b3819`; symmetric endpoint wiring; `5d1c8c0` fixes Host MCP's GET/DELETE 405 (was a fatal 501 to Codex's rmcp transport). | `Docs/Validation/TASK-016_CLAUDE_ADAPTER_PROOF.md` records both adapters' independent, same-session proofs against the same running `CotSToolLab` editor (`editor_pid 52020`): Host MCP connect/lock acquire-release, native Unreal MCP connect with matching project/engine identity, native and CotS toolset enumeration, one read-only inspection call through each server, and the single-mutating-agent lease rejecting each standby identity's Host MCP mutation attempt while its read-only status calls stayed unaffected. | None. |
| TASK-100 | COMPLETE_VERIFIED | Reconcile the production baseline and establish a shallow existing-work index across production, Shardlands, Website, Platform API and developer tooling. | `Docs/Production/EXISTING_WORK_INDEX.md` records production at `8e744c7`, local Shardlands at `64f5ca4` with its dirty/freshness caveat, task routing, and unavailable peer-repository caveats. | TASK-015's durable canonical editor/game build, native MCP, entry-map and smoke evidence remains valid; fixed production status confirms a clean production worktree. | None. Later tasks must perform their specified just-in-time donor and peer-contract inspection. |
| TASK-101 | PARTIAL | Production runtime/networking and dedicated-server foundation. | `Docs/Production/Reuse/TASK-101.md` classifies donor authority patterns for adaptation; production `89cacc6` adds the replicated, server-validated `ACotSNetworkProbeActor` and focused automation test. | Canonical production editor build returned UBT `Result: Succeeded`; live native-MCP Automation discovered and passed `CotS.Runtime.NetworkProbe.AuthorityContract` (1/1, no errors or warnings). | Add the real multi-client connect/spawn/replicate/disconnect/reconnect harness and obtain a server-capable engine for a clean dedicated-server build. |

`SUPERSEDED` is not used: every original foundation task remains a dependency
or a preserved capability. Production TASK-100 through TASK-115 are present in
the machine-readable state so their independently verified completion cannot be
mistaken for roadmap-wide completion.
